night writer needs to:

read a file of alphanumeric characters
save characters to a string
match each character to a braille character in a nested array
  of three lines in a hash (iteration)
check for output File
create if it doesn't exist
print the first nested hash keys
end line
print second nested hash keys
end line
print third nested hash keys
end line





files needed
1. file writer and reader
